<h1>Tasks#show - More detail about the task</h1>
<h1>Name of the task: <%= @task.name %></h1>
<h2>Description of the task: <%= @task.description %></h2>
<h2>Priority of the task: <%= @task.priority %></h2>

<%= link_to "Index", tasks_path %>
<%= link_to "Edit", edit_task_path(@task) %>
<%= link_to "delete task", task_path(@task), method: :delete, data: { confirm: "Are you sure?" } %>
